buildscript {
    apply from: "$projectDir/gradle-scripts/buildscript.gradle", to: it
}

apply from: "$projectDir/gradle-scripts/root.gradle"

subprojects {
    apply from: "$scriptsDir/java.gradle"
    apply from: "$scriptsDir/maven-publish.gradle"
    apply from: "$scriptsDir/bintray.gradle"
}
