buildscript {
    apply from: "$projectDir/gradle/buildscript.gradle", to: it
}

apply from: "$projectDir/gradle/root.gradle"

subprojects {
    apply from: "$scriptsDir/java.gradle"
    //apply from: "$scriptsDir/bintray.gradle"
    apply from: "$scriptsDir/sonatype.gradle"
}
